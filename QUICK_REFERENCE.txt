# ⚡ Quick Reference - Dương (Thành viên 4)\n\n## 🎯 7 Files cần làm\n\n| # | File | Công việc | Độ khó | Time |\n|-|------|----------|--------|------|\n| 1 | `include/mm.h` | Fix 2 macros | ⭐ | 5min |\n| 2 | `src/mm-vm.c` | 3 functions | ⭐⭐⭐ | 45min |\n| 3 | `src/sys_meminc.c` | Tạo mới | ⭐⭐ | 15min |\n| 4 | `src/sys_memswp.c` | Tạo mới | ⭐⭐ | 15min |\n| 5 | `src/syscall.tbl` | Thêm 2 lines | ⭐ | 5min |\n| 6 | `src/sys_mem.c` | Cập nhật | ⭐⭐ | 20min |\n| 7 | `Makefile` | (optional) | ⭐ | 5min |\n\n---\n\n## 🔧 Quick Fixes\n\n### FIX 1: include/mm.h (Line 72-73)\n\n```c\n// FROM:\n#define OVERLAP(x1,x2,y1,y2) (0)\n#define INCLUDE(x1,x2,y1,y2) (0)\n\n// TO:\n#define OVERLAP(x1,x2,y1,y2) ((x1 < y2) && (y1 < x2))\n#define INCLUDE(x1,x2,y1,y2) ((x1 <= y1) && (y2 <= x2))\n```\n\n---\n\n### FIX 2: src/mm-vm.c\n\n#### Function 1: `get_vm_area_node_at_brk()` (Lines 58-81)\n\n```c\nstruct vm_rg_struct *get_vm_area_node_at_brk(struct pcb_t *caller, \n                                             int vmaid, addr_t size, addr_t alignedsz)\n{\n  struct vm_area_struct *cur_vma = get_vma_by_num(caller->krnl->mm, vmaid);\n  if (cur_vma == NULL) return NULL;\n\n  struct vm_rg_struct *newrg = malloc(sizeof(struct vm_rg_struct));\n  if (newrg == NULL) return NULL;\n\n  newrg->rg_start = cur_vma->sbrk;\n  newrg->rg_end = newrg->rg_start + alignedsz;\n  newrg->rg_next = NULL;\n\n  cur_vma->sbrk = newrg->rg_end;\n  return newrg;\n}\n```\n\n#### Function 2: `validate_overlap_vm_area()` (Lines 84-125)\n- Sẽ hoạt động sau khi fix OVERLAP macro ✓\n\n#### Function 3: `inc_vma_limit()` (Lines 128-157)\n\n```c\nint inc_vma_limit(struct pcb_t *caller, int vmaid, addr_t inc_sz)\n{\n  addr_t inc_amt = PAGING_PAGE_ALIGNSZ(inc_sz);\n  int incnumpage = DIV_ROUND_UP(inc_amt, PAGING_PAGESZ);\n\n  struct vm_area_struct *cur_vma = get_vma_by_num(caller->krnl->mm, vmaid);\n  if (cur_vma == NULL) return -1;\n\n  addr_t old_end = cur_vma->vm_end;\n  addr_t new_end = old_end + inc_amt;\n\n  if (validate_overlap_vm_area(caller, vmaid, old_end, new_end) < 0)\n    return -1;\n\n  struct vm_rg_struct newrg;\n  newrg.rg_start = old_end;\n  newrg.rg_end = new_end;\n  newrg.rg_next = NULL;\n\n  if (vm_map_ram(caller, newrg.rg_start, newrg.rg_end, old_end, incnumpage, &newrg) < 0)\n    return -1;\n\n  cur_vma->vm_end = new_end;\n  return 0;\n}\n```\n\n---\n\n### FIX 3: Create src/sys_meminc.c (NEW FILE)\n\n```c\n#include \"syscall.h\"\n#include \"common.h\"\n#include \"os-mm.h\"\n#include \"mm.h\"\n#include <stdlib.h>\n\nint __sys_meminc(struct krnl_t *krnl, uint32_t pid, struct sc_regs* regs)\n{\n    int vmaid = regs->a1;\n    addr_t inc_sz = regs->a2;\n    \n    struct pcb_t *caller = NULL;\n    struct queue_t *running_list = krnl->running_list;\n    \n    if (running_list != NULL) {\n        struct qnode_t *qnode = running_list->head;\n        while (qnode != NULL) {\n            struct pcb_t *proc = (struct pcb_t *)qnode->data;\n            if (proc != NULL && proc->pid == pid) {\n                caller = proc;\n                break;\n            }\n            qnode = qnode->next;\n        }\n    }\n    \n    if (caller == NULL) return -1;\n    \n    int ret = inc_vma_limit(caller, vmaid, inc_sz);\n    regs->a3 = ret;\n    return ret;\n}\n```\n\n---\n\n### FIX 4: Create src/sys_memswp.c (NEW FILE)\n\n```c\n#include \"syscall.h\"\n#include \"common.h\"\n#include \"os-mm.h\"\n#include \"mm.h\"\n#include <stdlib.h>\n\nint __sys_memswp(struct krnl_t *krnl, uint32_t pid, struct sc_regs* regs)\n{\n    addr_t vicfpn = regs->a1;\n    addr_t swpfpn = regs->a2;\n    \n    struct pcb_t *caller = NULL;\n    struct queue_t *running_list = krnl->running_list;\n    \n    if (running_list != NULL) {\n        struct qnode_t *qnode = running_list->head;\n        while (qnode != NULL) {\n            struct pcb_t *proc = (struct pcb_t *)qnode->data;\n            if (proc != NULL && proc->pid == pid) {\n                caller = proc;\n                break;\n            }\n            qnode = qnode->next;\n        }\n    }\n    \n    if (caller == NULL) return -1;\n    \n    int ret = __mm_swap_page(caller, vicfpn, swpfpn);\n    regs->a3 = ret;\n    return ret;\n}\n```\n\n---\n\n### FIX 5: src/syscall.tbl (ADD 2 LINES)\n\n```plaintext\n0       listsyscall sys_listsyscall\n17      memmap      sys_memmap\n18      meminc      sys_meminc        ← ADD\n19      memswp      sys_memswp        ← ADD\n```\n\n---\n\n### FIX 6: src/sys_mem.c (UPDATE PROCESS FINDING)\n\n```c\nint __sys_memmap(struct krnl_t *krnl, uint32_t pid, struct sc_regs* regs)\n{\n    int memop = regs->a1;\n    BYTE value;\n    \n    // Find process by PID\n    struct pcb_t *caller = NULL;\n    struct queue_t *running_list = krnl->running_list;\n    \n    if (running_list != NULL) {\n        struct qnode_t *qnode = running_list->head;\n        while (qnode != NULL) {\n            struct pcb_t *proc = (struct pcb_t *)qnode->data;\n            if (proc != NULL && proc->pid == pid) {\n                caller = proc;\n                break;\n            }\n            qnode = qnode->next;\n        }\n    }\n    \n    if (caller == NULL) return -1;\n    \n    switch (memop) {\n    case SYSMEM_MAP_OP:\n        vmap_pgd_memset(caller, regs->a2, regs->a3);\n        break;\n    case SYSMEM_INC_OP:\n        inc_vma_limit(caller, regs->a2, regs->a3);\n        break;\n    case SYSMEM_SWP_OP:\n        __mm_swap_page(caller, regs->a2, regs->a3);\n        break;\n    case SYSMEM_IO_READ:\n        MEMPHY_read(caller->krnl->mram, regs->a2, &value);\n        regs->a3 = value;\n        break;\n    case SYSMEM_IO_WRITE:\n        MEMPHY_write(caller->krnl->mram, regs->a2, regs->a3);\n        break;\n    default:\n        return -1;\n    }\n    \n    return 0;\n}\n```\n\n---\n\n### FIX 7: Makefile (IF NEEDED)\n\nKiểm tra xem SYSCALL_OBJ có chứa sys_meminc.o sys_memswp.o không?\n\nNếu không, thêm vào:\n\n```makefile\nSYSCALL_OBJ = $(addprefix $(OBJ)/, syscall.o sys_mem.o sys_meminc.o sys_memswp.o sys_listsyscall.o)\n```\n\n---\n\n## 🧪 Compile & Test\n\n```bash\ncd ossim_lamiaatrium\nmake clean\nmake\n\n# Test\n./os < input/os_1_mlq_paging\n```\n\n---\n\n## 📊 Key Concepts\n\n### OVERLAP macro\n```c\nOVERLAP(x1, x2, y1, y2) checks if [x1,x2) overlaps [y1,y2)\n Example:\n - [0,10) vs [5,15) → OVERLAP = 1 (TRUE)\n - [0,10) vs [10,20) → OVERLAP = 0 (FALSE)\n```\n\n### Virtual Memory Area (VMA)\n```c\nstruct vm_area_struct {\n    unsigned long vm_id;        // 0, 1, 2, 3...\n    addr_t vm_start;            // Start address\n    addr_t vm_end;              // End address\n    addr_t sbrk;                // Break point (current)\n}\n```\n\n### Syscall Parameters\n```c\nstruct sc_regs {\n    arg_t a1, a2, a3;   // Input/Output parameters\n    // a1: param1, a2: param2, a3: param3/return value\n}\n```\n\n---\n\n## ✅ Final Checklist\n\n- [ ] Fix OVERLAP/INCLUDE macros in include/mm.h\n- [ ] Implement get_vm_area_node_at_brk() in mm-vm.c\n- [ ] Complete inc_vma_limit() in mm-vm.c\n- [ ] Create src/sys_meminc.c\n- [ ] Create src/sys_memswp.c\n- [ ] Update src/syscall.tbl\n- [ ] Update src/sys_mem.c\n- [ ] Update Makefile (if needed)\n- [ ] Run: `make clean && make`\n- [ ] Test: `./os < input/os_1_mlq_paging`\n- [ ] No segfault ✓\n- [ ] Submit ✓\n\n---\n\n**Total Time: ~2 hours**\n\n**Good luck! 🚀**\n